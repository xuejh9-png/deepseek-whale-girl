# WorkBuddy Pet — Character Asset Specification (v1)

> 面向「制作素材的人 / 模型」。按**动画剪辑**组织，不是按姿势图片。
> Runtime 侧已实现的部分只做**整图变换 + 帧序列播放**，所以下面每一条都以此为前提。

---

## 0. Runtime 能力边界（决定了素材该怎么出）

| 项 | 现状 | 对素材的要求 |
|---|---|---|
| 渲染 | WKWebView（桌面窗口 200×250 CSS px） | 浏览器支持的格式：**PNG 精灵图** |
| 动画方式 | 帧序列 + 整图 transform | **二级运动必须烘焙进帧里**（见 §7） |
| 图层 | v1 不分层 | 不要把头发/尾巴做成需要运行时分离 |
| 缩放 | 显示尺寸 = 画布 × 0.5 | 按 2× 出图，即 Retina 原生清晰 |
| 背景 | 窗口透明 | **必须 RGBA 透明底，不能有白底/棋盘格** |

---

## 1. 通用规范（所有剪辑统一）

| 项 | 规范 | 理由 |
|---|---|---|
| **单帧画布** | **384 × 512 px** | 3:4，够放跳跃 + 落地压缩的空间 |
| **锚点 / Pivot** | **双脚中心 = (192, 448)**，即 `x=50%, y=87.5%` | 全剪辑统一，避免切换时抖动 |
| **脚底以下留白** | 64 px | 落地 squash / 下蹲用 |
| **头顶以上留白** | 头顶距画布顶部 ≥ 56 px | 跳跃、举手、被抓起用 |
| **角色占画布比例** | 身高 ≈ 78%（站立时 ≈ 400 px），宽 ≈ 68% | 留出动作空间 |
| **显示缩放** | 0.5×（画布 → 130×190 CSS px 的角色） | 1:1 匹配 Retina |
| **背景** | 全透明 RGBA | 必须 |
| **色彩** | sRGB，不要嵌入式 ICC 之外的色彩配置 | 与 CSS 一致 |
| **边缘** | 干净描边，**不要外发光 / 投影 / 光晕** | 光晕会抠不干净，投影由 CSS 加 |
| **命名** | 全小写英文，见 §6 | |

### 精灵图排列

- **一行一个剪辑，水平条带**；超过 8 帧则换行
- **每行最多 8 帧**（8 × 384 = 3072 px，安全宽度）
- 帧与帧之间**无间隙**，从左到右、从上到下
- 例：12 帧 → 2 行（第 1 行 8 帧，第 2 行 4 帧），第二行左侧补透明

> 只要遵守「每行 8 帧、无间隙、RGBA、384×512」，切片就无需额外信息。
> 另附 `manifest.json` 描述帧数与 FPS（见 §6）。

---

## 2. 动画清单总表

| # | 剪辑名 | 帧数 | FPS | 时长 | 循环 | 优先级 | 用途 |
|---|---|---|---|---|---|---|---|
| 1 | `idle` | 16 | 8 | 2.0s | ✓ | **P0** | 默认状态 |
| 2 | `blink` | 4 | 20 | 0.2s | ✗ | **P0** | 插播，叠加在 idle 上 |
| 3 | `click` | 8 | 12 | 0.67s | ✗ | **P0** | 被点击的反应 |
| 4 | `jump` | 10 | 14 | 0.71s | ✗ | **P0** | 开心 / 成功跳跃 |
| 5 | `grabbed` | 4 | 10 | 0.4s | ✗ | **P0** | 被"抓起"的瞬间 |
| 6 | `dragging` | 6 | 10 | 0.6s | ✓ | **P0** | 拖动持续中 |
| 7 | `landing` | 6 | 14 | 0.43s | ✗ | **P0** | 释放后落地 |
| 8 | `run` | 12 | 16 | 0.75s | ✓ | **P0** | 位移（左右共用，见 §5） |
| 9 | `think` | 12 | 8 | 1.5s | ✓ | **P0** | Agent reasoning |
| 10 | `work` | 12 | 10 | 1.2s | ✓ | **P0** | Agent / Tool 执行中 |
| 11 | `success` | 14 | 12 | 1.17s | ✗ | **P0** | 任务完成 |
| 12 | `error` | 12 | 10 | 1.2s | ✗ | **P1** | 出错 |
| 13 | `sleep` | 12 | 6 | 2.0s | ✓ | **P1** | 长时间无活动 |
| 14 | `sit` | 12 | 6 | 2.0s | ✓ | **P2** | idle 变体（偶尔坐下） |

**合计**：P0 = 100 帧 / 11 张图；全量 = 146 帧 / 14 张图。

---

## 3. 帧相位分解（每个剪辑的动作连续性）

> 「帧」从 1 开始。所有剪辑的**首帧**都应与 `idle_001` 姿态接近，**尾帧**回到 `idle_001` 姿态
> （循环剪辑除外），这样衔接不会跳。

### 1) `idle` — 16 帧 / 8fps / 循环

一个 2 秒的完整呼吸周期，**内含 1 次眨眼 + 1 次尾巴摆动**。

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | 起 | 基准站姿，重心略偏一侧 |
| 002–004 | 吸气上升 | 胸口抬起，肩微沉，头发末端开始上扬 |
| 005–006 | 顶点 | 最高点，**瞳孔闭合（眨眼 1/2）** |
| 007 | 眨眼完成 | 睁眼 |
| 008–010 | 呼气下降 | 身体回落，**尾巴向右摆** |
| 011–012 | 最低点 | 略下沉 |
| 013–014 | 回弹 | 尾巴回摆 |
| 015–016 | 归位 | 回到 001 姿态，无缝循环 |

> **注意**：这是唯一允许"含眨眼"的剪辑；其余剪辑不要自行加眨眼。

### 2) `blink` — 4 帧 / 20fps / 不循环

| 帧 | 相位 |
|---|---|
| 001 | 眼睛半闭（上眼睑下降 50%） |
| 002 | 全闭 |
| 003 | 微睁 |
| 004 | 睁开（= idle_001） |

**身体姿态与 idle_001 完全一致**，只改眼部 —— 这样插播时不会看到身体跳动。

### 3) `click` — 8 帧 / 12fps / 不循环

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | Idle | = idle_001 |
| 002 | 受惊 | 身体瞬间收缩（scaleY 0.94），眼睛睁大 |
| 003–004 | 反应 | 双手微抬，头微歪，表情变开心 |
| 005–006 | 回落 | 身体放松 |
| 007–008 | 恢复 | 回到 idle_001 |

### 4) `jump` — 10 帧 / 14fps / 不循环

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | Anticipation 起始 | = idle_001 |
| 002–003 | Anticipation 下蹲 | 屈膝，身体压扁（squash），手往后 |
| 004 | Takeoff | 腿蹬直，身体拉长（stretch），**脚即将离地** |
| 005–006 | Airborne 上升 | 双脚离地，手臂上扬，头发/裙摆向上拖 |
| 007 | Apex | 最高点，身体略呈弧形，尾巴上扬 |
| 008–009 | Airborne 下落 | 头发/裙摆继续上飘（**滞后**），脚开始下探 |
| 010 | Landing 接触 | 脚触地，身体开始压缩 |

> `jump` 之后**必须接 `landing`**（由 runtime 串起来），不要在本剪辑里做落地压缩。

### 5) `grabbed` — 4 帧 / 10fps / 不循环（尾帧即 dragging 首帧）

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | 被抓住 | = idle_001，但重心上提 |
| 002 | 起吊 | 脚离地，身体轻微收缩（被提起的紧张感） |
| 003 | 悬空 | 双腿自然下垂微弯，手微张 |
| 004 | 稳定 | = `dragging_001` |

### 6) `dragging` — 6 帧 / 10fps / 循环

悬空姿态的 0.6 秒循环：身体微摆、**头发与尾巴下垂并缓慢摆动**（体现惯性）。
方向感应（往哪边拖就倾向哪边）由 runtime 用 `rotate` 做，**素材本身画中性悬垂即可**。

### 7) `landing` — 6 帧 / 14fps / 不循环

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | Contact | 脚触地，身体中立 |
| 002–003 | Squash | **最大压缩**（scaleY ~0.88，横向略宽） |
| 004–005 | Rebound | 反弹过冲，身体略拉长 |
| 006 | Settle | 回到 idle_001 |

### 8) `run` — 12 帧 / 16fps / 循环（**一个完整跑步周期 = 两步**）

标准跑步循环，右向。水平翻转即左向。

| 帧 | 相位（以"接触"为界） |
|---|---|
| 001 | Contact（右脚着地） |
| 002 | Down（重心最低，支撑腿弯） |
| 003 | Passing（支撑腿直立，另一腿收拢） |
| 004 | Up（重心最高，开始腾空） |
| 005 | Airborne（双脚离地） |
| 006 | Contact（左脚着地，进入第二步） |
| 007–010 | Down / Passing / Up / Airborne（镜像） |
| 011–012 | 过渡收尾，接回 Contact |

要点：
- **身体前倾** 5~8°，视线朝跑动方向
- 手臂前后摆，**幅度不要过大**（chibi 比例）
- 头发与裙摆**滞后 1 帧**（即尾部飘动方向比身体晚一拍）
- 锚点不变，脚底允许短暂离地（画布下方 64px 就是给这个的）

### 9) `think` — 12 帧 / 8fps / 循环

| 帧 | 相位 |
|---|---|
| 001 | 基准站姿 |
| 002–004 | 头微歪，视线偏上，一手托腮或抬到胸前 |
| 005–008 | 保持思考姿态，**尾巴缓慢左右摆**，轻微上下浮动 |
| 009–012 | 头回到正中再微歪，形成循环 |

### 10) `work` — 12 帧 / 10fps / 循环

| 帧 | 相位 |
|---|---|
| 001 | 抱出一台小笔记本电脑 |
| 002–004 | 坐下 / 半蹲，把电脑放稳 |
| 005–010 | **打字循环**：手指上下起伏，头部微低看屏幕，每 2 帧一个节拍 |
| 011–012 | 略微抬头看一眼，循环接回 005 |

### 11) `success` — 14 帧 / 12fps / 不循环

| 帧 | 相位 |
|---|---|
| 001 | = idle_001 |
| 002–003 | 蓄力下蹲 |
| 004–008 | **跳起 + 举手**（可复用 jump 的腾空段，但手臂高举） |
| 009–011 | 落地压缩 |
| 012–013 | 开心笑容，**头顶可带 2~3 颗小星星**（画在帧里，不要让 runtime 生成） |
| 014 | 回到 idle_001 |

### 12) `error` — 12 帧 / 10fps / 不循环（P1）

| 帧 | 相位 |
|---|---|
| 001 | = idle_001 |
| 002–003 | 愣住（身体僵一下） |
| 004–006 | 头顶冒出 `!`，眼睛变成困惑状 |
| 007–009 | 歪头，双手微摊（「诶？」） |
| 010–012 | 回到 idle_001 |

### 13) `sleep` — 12 帧 / 6fps / 循环（P1）

| 帧 | 相位 |
|---|---|
| 001–004 | 打哈欠，闭眼 |
| 005–008 | 头垂下，身体放松下沉 |
| 009–012 | 缓慢呼吸起伏（幅度比 idle 大），尾巴偶尔抖一下 |

### 14) `sit` — 12 帧 / 6fps / 循环（P2，可选）

坐姿呼吸循环，作为 idle 的偶发变体。

---

## 4. 透明与边缘（最容易出问题的地方）

❌ **不要**：
- 白色底 / 灰色底 / 棋盘格（棋盘格最危险 —— 它会被当成真实像素留下来）
- 角色外圈的柔光、光晕、渐隐阴影（渐变抠不干净）
- 投影（阴影由 CSS 的 `drop-shadow` 生成）

✅ **要**：
- 完全透明的背景
- 清晰的角色描边
- 半透明区域（如发梢）保留正确的 alpha 值

> 实测说明：图像模型常把「透明背景」理解成画出棋盘格。
> **请导出自带 alpha 通道的 PNG**，或先出纯平面白底、由我统一抠图（但白底不能有光晕）。

---

## 5. 左右复用规则

| 剪辑 | 需要左右两套吗 | 说明 |
|---|---|---|
| `run` | **否** | 只出**右向**一套，左向由 runtime `scaleX(-1)` 水平翻转 |
| 其余全部 | **否** | 均为正面/对称姿态，单套即可 |

> 所以**总共产出 14 个剪辑，不需要 ×2**。

---

## 6. 文件与目录规范

```
assets/pet/
  manifest.json      # 描述所有剪辑（帧数 / FPS / 循环 / 网格 / 锚点）
  idle.png
  blink.png
  click.png
  jump.png
  grabbed.png
  dragging.png
  landing.png
  run.png
  think.png
  work.png
  success.png
  error.png
  sleep.png
  sit.png            # 可选
```

`manifest.json` 格式（我会据此接入，你可以直接照填）：

```json
{
  "version": 1,
  "canvas": { "w": 384, "h": 512 },
  "anchor": { "x": 0.5, "y": 0.875 },
  "displayScale": 0.5,
  "columns": 8,
  "clips": {
    "idle":     { "frames": 16, "fps": 8,  "loop": true,  "row": 0 },
    "blink":    { "frames": 4,  "fps": 20, "loop": false, "row": 0 },
    "click":    { "frames": 8,  "fps": 12, "loop": false, "row": 0 },
    "jump":     { "frames": 10, "fps": 14, "loop": false, "row": 0 },
    "grabbed":  { "frames": 4,  "fps": 10, "loop": false, "row": 0 },
    "dragging": { "frames": 6,  "fps": 10, "loop": true,  "row": 0 },
    "landing":  { "frames": 6,  "fps": 14, "loop": false, "row": 0 },
    "run":      { "frames": 12, "fps": 16, "loop": true,  "row": 0, "flipForLeft": true },
    "think":    { "frames": 12, "fps": 8,  "loop": true,  "row": 0 },
    "work":     { "frames": 12, "fps": 10, "loop": true,  "row": 0 },
    "success":  { "frames": 14, "fps": 12, "loop": false, "row": 0 },
    "error":    { "frames": 12, "fps": 10, "loop": false, "row": 0 },
    "sleep":    { "frames": 12, "fps": 6,  "loop": true,  "row": 0 },
    "sit":      { "frames": 12, "fps": 6,  "loop": true,  "row": 0 }
  }
}
```

---

## 7. 二级运动：必须烘焙进帧里

v1 runtime 不做分层，**头发 / 裙摆 / 尾巴的滞后与跟随要画在帧序列里**。具体：

| 场景 | 要求 |
|---|---|
| idle | 头发末端比身体晚 1 帧到达最高点；尾巴独立摆动 |
| jump | 上升时头发/裙摆**向下拖**，下落时**向上飘**（反向滞后） |
| landing | 落地瞬间**裙摆还在往下压**，比身体晚 1~2 帧才回弹 |
| dragging | 头发与尾巴**下垂**，并比身体晚 2 帧跟着摆动 |
| run | 头发/裙摆飘动方向比身体晚 1 帧 |

> 这是「活起来」和「一张纸在晃」的分界线。**请把这条当作硬要求。**

---

## 8. 验收标准（我拿到素材后会逐条检查）

1. 全部 PNG 为 **RGBA**，四角 `alpha = 0`（无白底、无棋盘格、无光晕残留）
2. 每张画布 **严格 384×512**，帧数符合 §2
3. 所有帧的角色**脚底落在 y=448±4**，水平中心落在 x=192±6（锚点一致）
4. 剪辑首尾帧能自然衔接（循环剪辑首尾同姿）
5. `idle` 肉眼可见呼吸 + 尾巴摆动 + 1 次眨眼
6. `run` 有明确的两步循环与腾空帧
7. `jump` 有下蹲 / 拉长 / 腾空 / 触地四个可辨阶段
8. 二级运动在 `jump` / `landing` / `dragging` 中肉眼可辨

有任何一条不满足，我会指出来，**不会用低质量素材硬凑**。

---

## 9. 交付优先级

| 批次 | 内容 | 说明 |
|---|---|---|
| **第 1 批** | `idle` `blink` `think` `work` | 4 个剪辑就能跑通「平时活着 + 两种工作状态」 |
| **第 2 批** | `click` `jump` `grabbed` `dragging` `landing` | 交互层 |
| **第 3 批** | `run` `success` `error` `sleep` | 位移与结果反馈 |
| 可选 | `sit` | idle 变体 |

**先给我第 1 批就能开始接 Runtime**，不必等全部齐。
