# ASSET_SPEC — 运行时契约（内部）

> **外发给生成模型的素材规格不在这里。**
> 完整 brief（角色身份 / 视觉风格 / 比例 / 逐剪辑帧相位 / 英文 prompt / 验收）在：
>
> ```
> ~/Desktop/WorkBuddy桌宠_素材规格_给GPT.md
> ```
>
> 本文件只记录**代码实际依赖的契约** —— 换素材时对着这份改，不会漏掉运行时那一侧。

---

## 一、代码依赖的几何参数

改素材几何时，以下位置**必须同步**，否则角色会错位或大小不对：

| # | 位置 | 参数 | 当前值 |
|---|---|---|---|
| 1 | `web/pet.css` `:root` | `--pet-char-height` | `128px` |
| 2 | `web/pet.css` `:root` | `--pet-char-in-canvas` | `0.64` |
| 3 | `web/pet.css` `:root` | `--pet-aspect` | `0.8` |
| 4 | `desktop/main.swift` | `winW` / `winH` | `160` / `200` |
| 5 | `desktop/main.swift` | 鼠标命中区（`updatePassthrough`） | `x23% / y7% / w54% / h70%` |

推导关系（别手算，改 1–3 即可）：

```
--pet-canvas-height = --pet-char-height / --pet-char-in-canvas     (= 200px)
窗口宽             = --pet-canvas-height × --pet-aspect             (= 160px)
窗口高             = --pet-canvas-height                            (= 200px)
```

**素材侧对应值**：画布 320×400，角色 256px（64%），锚点 `(160, 360)`，
显示缩放 0.5×。选 320×400 / 0.5× 是为了在 2× Retina 上
`128 CSS px = 256 设备像素 = 画布角色高度`，像素级对齐最清晰。

---

## 二、代码依赖的文件约定

| 项 | 约定 |
|---|---|
| 目录 | `assets/pet/` |
| 命名 | `<clip>.png`，小写；如 `idle.png` / `think.png` |
| 每张图 | 一个剪辑的 **Sprite Sheet**，固定 6 列、行优先、帧间无间隙 |
| 单格 | 320 × 400，RGBA 透明 |
| 清单 | `assets/pet/manifest.json`（见第三条） |
| 当前状态 | 第 1、2 批共 6 个剪辑已交付并验收；第 3 批 4 个剪辑在磁盘上但未入清单（见第四条） |

### ⚠️ manifest 是累积的，**不能按批覆盖**

制作方每一批只登记**自己那批**的剪辑（第 1 批交来的清单里只有 idle/think/work）。
如果第 2 批的 manifest 直接覆盖，**旧剪辑会从清单里消失**，桌宠会退化成只剩新动作。

正确做法：

- **Sprite PNG 可以直接覆盖进 `assets/pet/`**（文件名不冲突）
- **manifest 必须合并**：保留已有 `clips` 条目，只增补新批次的条目
- 合并是运行时这一侧的职责，不是素材方的

合并后建议跑一次 `pet-runtime.test.html` + 逐帧几何验收再接入。

### 锚点语义（别搞错）

`(160, 360)` 是**逻辑地面锚点**，不是"每帧脚底都钉在这里"：

- **接地帧**：脚底 `y = 360 ± 5`
- **离地帧**（`jump` 腾空 / `drag` 悬吊 / `run` 腾空）：脚底**必须高于 360**
- **任何帧**：脚底**不得低于** 360（那是地面）

否则 `jump` / `drag` 的动作定义会和锚点约束直接打架。

---

## 三、manifest.json 关键字段

运行时后续会读它，字段语义：

| 字段 | 位置 | 说明 |
|---|---|---|
| `frameWidth` / `frameHeight` | `spriteSheet` | 单格尺寸 |
| `columns` | `spriteSheet` | 固定 6 |
| `anchorX` / `anchorY` | `spriteSheet` | **归一化** 0–1（0.5 / 0.9）；画布换尺寸时自动跟随 |
| `groundLineY` | `spriteSheet` | 逻辑地面线（同 `anchorY`） |
| `frameCount` / `rows` | 每剪辑 | `rows = ceil(frameCount / columns)`，显式写出便于校验 |
| `fps` / `loop` | 每剪辑 | |
| `loopStart` / `loopEnd` | 每剪辑 | **1 基**帧号；`loop=false` 时为 `null` |
| `airborneFrame` | `jump` | 腾空最高点帧号（拖动释放时从这一帧起播） |
| `flipForLeft` | `run` | `true` = 左向由程序水平翻转 |

**`loopStart` / `loopEnd` 是必须的**：`drag` / `think` / `work` / `sleep`
的开头几帧是"进入状态"的一次性过渡，整段循环会导致反复重播进入动作（看起来像卡带）。

| 剪辑 | 开场 | 循环段 |
|---|---|---|
| `drag` / `think` / `work` | 1–4 | 5–12 |
| `sleep` | 1–8 | 9–12 |

完整 schema 见外发 brief 第 11 节。

---

## 四、素材现状 —— **10 个剪辑已全集**

`assets/pet/` 的 10 个 PNG 与 `manifest.json` 的 10 条**已完全一致**，
运行时六个状态（idle / thinking / working / success / error / sleeping）全部有对应剪辑。

| 文件 | 尺寸 | 帧 | 类型 | 验收 |
|---|---|---|---|---|
| `idle.png` | 1920×1600 | 20（6×4） | 循环 1–20 | ✓ 20/20 姿态，接缝 0.00 |
| `think.png` | 1920×800 | 12（6×2） | 循环 5–12 | ✓ 7/8 姿态，接缝 0.00 |
| `work.png` | 1920×800 | 12（6×2） | 循环 5–12 | ⚠ **待二次返工**：接缝 0.00、姿态 8/8，但显示尺寸下每帧只变 8~14px（**FAIL**）|
| `click.png` | 1920×800 | 8（6×2） | 一次性 | ✓ 首尾↔idle#1 均 0.00 |
| `jump.png` | 1920×1200 | 14（6×3） | 一次性，`airborneFrame: 7` | ✓ 离地 005–009（脚底 323→271→332） |
| `drag.png` | 1920×800 | 12（6×2） | 开场 1–4 + 循环 5–12 | ✓ 8/8 姿态；001 接地、002 起离地 |
| `run.png` | 1920×800 | 12（6×2） | 循环 1–12 | ✓ 12/12 姿态；离地 005/010（脚底 318） |
| `success.png` | 1920×1200 | 14（6×3） | 一次性 | ✓ 首尾↔idle#1 均 0.00；离地 004–008 |
| `error.png` | 1920×800 | 12（6×2） | 一次性 | ⚠ 可用，见「已知短板」 |
| `sleep.png` | 1920×800 | 12（6×2） | 开场 1–8 + 循环 9–12 | ✓ 循环段 4/4 姿态，接缝 0.00 |
| `manifest.json` | — | — | schema **v5**，含全部 10 个剪辑 | ✓ |

### 验收结论（`verify-assets.py`，9 支通过 / 1 支 FAIL）

| 项 | 结果 |
|---|---|
| 尺寸 / 行列自洽 / RGBA | 全部通过 |
| 四角透明、背景无棋盘格 | 全部通过 |
| 水平中心、宽度预算（≤205） | 全部通过 |
| 接地帧相对基准（y=358）偏离 | 无超限 |
| **离地帧** | run 005/010、jump 005–009、drag 002–012、success 004–008 全部正确离地 |
| **循环接缝** | 6 支有循环段的剪辑全部 **0.00** |
| **循环段可见性**（显示尺寸 160×200，下限 40px） | 9 支通过；`work` **8~14px → FAIL，待返工** |
| **一次性剪辑首尾** | click / jump / success / error 首尾↔idle#1 全部 0.00 |
| 运行时回归（`pet-runtime.test.html`） | **PASS=28 / FAIL=0**（无头实测） |
| 状态推断回归（`pet_sources.test.py`） | **13/13 通过**（实测） |
| 画面几何 | 角色显示高 129px、水平居中 ✓ |

### 已知短板

- **`work` 循环段：先「动错部位」、再「几乎不动」—— 两次都没踩对（2026-09-23）**：
  - **旧版**：整个人都在动（变化区域 **204×259px**、占画布 28.6%，竖直方向全包）
    → 被眼睛读成"站起来"，语义错。
  - **返工版**：把运动收缩到**手部 125×44px、占比 0.37%**
    → 语义对了，但幅度做死了：缩到实际显示尺寸（160×200，画布 ×0.5）后每帧只剩
    **8~14 个屏幕像素**在动，低于人眼可察觉阈值，运行时**看起来就是完全静止**。
    （用户原话："这个东西完全不动了，很出戏"。）

  ⚠️ **两次修正的病根是同一个：都在「幅度」上找答案，没在「运动模式」上找。**

  **正确做法（2026-09-23 用户定调）—— 不要抠手指：**

  > "她其实只要动就行，比如摇头晃脑，然后电脑随便晃一下也行……
  >  Q 版本来手就没有多仔细。"

  Q 版角色的辨识度来自**轮廓与头身比**，不来自手指。硬做"逐帧敲键盘"既难做、
  又留不住观感（返工版就是反证）。改用**头部 / 上半身的节奏性动作**表达"在忙"：

  - **要**：头部点头摇头、上半身节奏性摆动、桌上道具（电脑 / 杯子）轻微位移
  - **不要**：手指尖几个像素的抖动 —— 做不细，也看不见
  - **不要**：整个轮廓竖直大位移 —— 会被读成"站起来 / 走开"

  **量化判据（只有一条硬的）：**

  - **看不见 = 不合格**：实际显示尺寸（160×200）下，循环段每帧变化 **≥40px**
    （≈ 原画布 160px）。`verify-assets.py` 已按此判定，低于即 FAIL。
    → 落到返工上就一句话：**把现有动作幅度放大约 3~5 倍**
    （现在原始画布相邻帧只变 26~73px，需要 ≥160px）。

  ⚠️ **别用"变化区域 bbox 高度"去判"站起来"** —— 2026-09-23 实测过，这条路不通：

  | 剪辑 | 包络宽 | 包络高 | 高 / 角色 |
  |---|---|---|---|
  | idle | 204 | 258 | 101% |
  | think | 204 | 258 | 101% |
  | sleep | 204 | 261 | 102% |
  | drag | 214 | 260 | 102% |
  | run | 209 | 294 | 115% |
  | **work** | **125** | **44** | **17%** |

  正常剪辑的包络**全都 ~100% 角色高**（循环段内呼吸/形变会让掩码并集覆盖全身），
  而旧版"站起来"的 work 是 204×259 —— **两者尺寸几乎一样，bbox 区分不出来**。
  真正区分「起身」与「摆动」的是**轮廓是否整体平移**（脚底 / 水平中心是否漂移），
  这一点脚本原有的「接地帧脚底 ±8、水平中心 ±8」已经管住了，不需要新判据。

  > 本条的教训：**没量过的阈值不要写成判据。**
  > 这类"防起身"的阈值最容易凭直觉拍（例如"变化高度不超过角色高 60%"），
  > 一量就发现会误伤全部正常剪辑 —— **判据写错比没有判据更糟，它会引发错误返工。**
- **`error` 有 6 组相邻帧完全相同**（1=2、2=3、4=5、5=6、10=11、11=12），
  12 帧里实际只有约 6 个姿态；**前 3 帧完全一样**，与规格「002–003 愣住（身体僵一下）」
  的差异不明显。部分属设计（"愣住"本就是静止），但前段偏弱。
- `error` 第 4–6 帧高度 329（+73）—— 是**头顶的 `!` 符号**，属规格要求，不是缺陷；
  验收脚本的高度容差不知道这个特例，会报 WARN。

### 通用原则：状态动作走「整体节奏」，不走「肢体细节」（2026-09-23）

从 `work` 的两次返工里提炼，适用于**所有**状态剪辑：

| 状态 | 用什么动作表达 |
|---|---|
| 在忙（work / thinking） | 上半身 + 头的节奏性摆动，可带桌上道具轻微位移 |
| 思考（think） | 头部轻晃 / 歪头 |
| 睡觉（sleep） | 整体呼吸起伏 |

**预算花在「大轮廓的运动」上。** 两个反例都要避免：

- 只动手指尖 → 做不细，且幅度太小被运行时读成**静止**
- 整个人竖直位移 → 会被读成**起身 / 走开**，而不是"在工作"

一句话：**要的是局部摆动，不是全身平移，也不是末端抖动。**

### ⚠️ 验收脚本的灵敏度（2026-09-23 修正，共两刀）

**第一刀：「有没有变」不能用全图平均像素差。**
它会被大面积静止区域稀释：`work` 返工后手指每帧只变 26~73 个像素，
平均差 <0.5，会被误判成"只有 1 个姿态 → 近似二值切换"（**误报**）。
正确做法：**数「变化的像素个数」**（单像素变化 >8 视为变了，一帧至少 20 个像素变了才算姿态不同）。

**第二刀：「看不看得见」不能用原画布像素数。**
只数原画布像素，会放过那种**做废了的动作** —— work 的 26~73px > 20 阈值，
于是"8/8 姿态"全部通过验收；但缩到实际显示尺寸（160×200）后只剩 **8~14px**，
运行时就是静止的。验收表上完美，桌面上是死的。
正确做法：**按实际显示尺寸（画布 ÷2 = 160×200）再量一遍**，
循环段单帧变化低于 `MIN_VISIBLE_PX = 40` 直接判 FAIL。
实测余量：work 8~14（FAIL）、idle 281~5257、think 0~8472、run 2405~9815、
drag 6628~7891、sleep 4393~6486 —— 阈值落在中间，不误伤。

> **归纳：一次合格的验收必须同时回答两个问题 ——「有没有变」和「看不看得见」。**
> 只回答前者，产出的动画会在验收表上全绿，而在桌面上不动。

---

## 五、素材接入规范（血泪教训）

### 硬规则一：只自动新增，**永不自动覆盖**

`intake-assets.py` 把交付文件分成三类：

| 分类 | 判定 | 动作 |
|---|---|---|
| `new` | 项目里没有同名文件 | **安装** |
| `same` | 字节完全相同 | 跳过 |
| `differs` | 项目已有同名文件但内容不同 | **不安装**，写通知交人确认 |

**为什么**：2026-09-22 夜里，初版脚本只判断「交付物与项目是否不同」、
**没判断「谁更新」** —— 桌面上的旧包把项目里刚交付的**返工版 `work.png` 降级覆盖，无备份**。

根因是方法错不是疏忽：**一个只会"发现差异就写入"的自动化，
在项目比来源更新时必然造成回退。**
（也不要试图用文件 mtime 判新旧 —— zip 里的时间戳是压缩时的，不可信。）

### 硬规则二：安装前先备份

备份到 `assets/.backup/<时间戳>/`（已加入 `.gitignore`）。

这条**真的救过命**：另一次覆盖事故中，返工版就是从备份里找回来的。

### 硬规则三：不通过绝不安装

先在临时目录里验「**合并后的全集**」，验收通过才动项目。
素材不合格时项目保持原样，不会被半成品污染。

### manifest 是累积的，不能按批覆盖

制作方每批只登记自己那批的剪辑，直接覆盖会让旧剪辑从清单消失。
`intake-assets.py` 用**合并**语义：保留已有条目，只增补/更新新条目。

## 六、换素材后的验证流程

素材放进 `assets/pet/` 之后，按顺序跑：

```bash
# 1. 起服务（必须与后续 curl / 截图在同一条命令里：后台进程会随 shell 结束）
python3 pet_daemon.py --port 8791 &
sleep 3

# 2. 接口自检
curl -s http://127.0.0.1:8791/health
curl -s http://127.0.0.1:8791/state      # 应只有 agent，无 usage
```

几何自检（无头截图 → 量像素）：

> ⚠️ **Chrome 无头有最小窗口宽度约 500px。**
> 用 <500 的宽度截图，拿到的是大视口的**左侧裁切**，角色会"看起来跑到右边"，
> 极易误判成 CSS bug。必须用 ≥500 的宽度截图。

| 断言 | 期望 |
|---|---|
| 角色显示高度 | 127~128 px（目标 120~130） |
| 左右留白 | 差值 ≤ 2 px（水平居中） |
| 距窗口底 | ≈ 20 px（= 40/400 × 200） |
| 角色宽 | ≤ 103 px（= 205/320 × 160） |

素材本身的 12 条验收标准，见外发 brief 第 12 节。
