# WorkBuddy Pet — Character Asset Specification (v2)

> 面向「制作素材的人 / 模型」。**按动画剪辑组织**，不是姿势图片。
> v2 相对 v1 的变化：渲染尺寸与画布几何重定、身体比例收敛到 Desktop Mascot 比例、
> 剪辑清单对齐到 10 个。
>
> **v2 全表以此为唯一依据，v1 的旧数值（384×512 画布等）全部作废。**

---

## 0. 两个必须先分清的概念

| 概念 | 值 | 谁决定 |
|---|---|---|
| **Asset Resolution（素材分辨率）** | 单帧画布 **256 × 320** | 出图时定，保证 Retina 清晰 |
| **Rendered Size（屏幕渲染尺寸）** | 角色本体 **104 CSS px 高** | 运行时一个配置项决定 |

**不要因为画布是 256×320 就按原始像素显示。** 两者完全解耦：
画布按 2× 于渲染尺寸出图，屏幕上只占 128×160 CSS px。

改大小的唯一旋钮（我这边已实现，你不需要管）：

```
web/pet.css  →  --pet-char-height: 104px;     /* 目标区间 96~112 */
desktop/main.swift → winW = 128, winH = 160   /* 画布显示尺寸 */
```

**当前版本不提供大小调节 UI**，只保留这个默认值。

---

## 1. 画布与锚点（所有剪辑统一，不得逐帧变化）

| 项 | 规格 |
|---|---|
| **单帧画布** | **256 × 320 px**（4:5） |
| **锚点 / Pivot** | **双脚中心 = (128, 288)**，即 `x = 50%, y = 90%` |
| **角色高度** | **208 px = 画布的 65%** |
| **脚下留白** | **32 px**（画布底 → 脚底）→ 落地压缩、下蹲用 |
| **头顶留白** | **80 px**（头顶 → 画布顶）→ 跳跃、举手、被抓起用 |
| **显示缩放** | **0.5×** → 画布显示为 128×160 CSS px，角色 = 104 CSS px |
| **背景** | 全透明 RGBA，四角 `alpha = 0` |
| **对齐要求** | **所有帧**脚底落在 `y = 288 ± 4`，水平中心落在 `x = 128 ± 6` |

> 锚点统一是硬要求。锚点一飘，切换剪辑时角色就会上下跳。

### 精灵图排列

- 一行一个剪辑，**水平条带，每行最多 8 帧**，超出换行
- 帧与帧**无间隙**，从左到右、从上到下
- 每帧严格 256×320；12 帧 → 2 行（8 + 4），第二行右侧补透明

---

## 2. 身体比例（这一版最重要的改动）

当前素材是「小型动漫人物」，偏 2.5~3 heads。**要收敛到 Desktop Mascot 比例：**

### 目标：整个角色只有 **1.7 ~ 1.9 个头高**

以 `H = 208 px`（角色总高）为基准：

| 部位 | 占比 | 像素 | 说明 |
|---|---|---|---|
| **头**（颅顶 → 下巴，含头发体积） | **0.55 H** | **≈ 114 px** | 非常大、圆润，**视觉中心** |
| **躯干**（下巴 → 胯） | **0.25 H** | **≈ 52 px** | 明显缩短，**不要腰身、不要长躯干** |
| **腿 + 鞋**（胯 → 脚底） | **0.20 H** | **≈ 42 px** | 非常短 |
| 合计 | 1.00 H | 208 px | → **1.82 heads** ✓ |

### 横向与四肢

| 项 | 规格 | 像素 |
|---|---|---|
| 头宽 | ≈ 0.52 H（接近头高，圆润） | ≈ 108 px |
| 肩宽 | ≈ 0.30 H | ≈ 62 px |
| 手臂长度 | ≈ 0.18 H（**短、小、圆**） | ≈ 37 px |
| 手臂粗细 | ≈ 0.055 H | ≈ 11 px |
| 手 | 小圆球 | 直径 ≈ 15 px |
| 鞋长 | 小 | ≈ 21 px |
| **整体 bounding box 宽** | **≤ 0.80 H** | **≤ 166 px**（画布 256 的 65%） |

**禁止**：正常人体比例的长手臂、明显腰线、修长腿。不能看起来像"缩小的人类"。

### 头发（关键，容易把角色撑大）

- 保留蓝色长发 identity，但**压缩长度与横向展开**
- **发梢末端不得低于「脚底往上 0.15 H」**（≈ 31 px）—— 即**不要拖到脚**
- **横向展开 ≤ 0.42 H**（每侧 ≤ 0.21 H）
- 后发的"体积"计入头高，但飘散的细发丝不计入 bounding box 预算

### 尾巴（Species Identity，允许略大）

- 可以比身体略大 —— 这是最重要的识别特征
- 但**横向展开 ≤ 0.35 H**，**垂直高度 ≤ 0.25 H**
- **不允许尾巴把整体 bounding box 撑宽**（尾巴要收在 0.80 H 的预算内）

### 目标感觉

> 一只**很小、头很大、身体很短**的 3D Anime Whale Girl Mascot。
> 不是缩小版动漫人物，也不是桌面手办。
> `Codex Pet 的紧凑度` + `鲸鱼娘的角色 identity` + `Stylized 3D Anime Chibi`

---

## 3. 小尺寸可读性（104 px 下的硬约束）

角色在屏幕上只有 104 px 高，**动画不能依赖细微动作**。换算关系：
**1 画布 px ≈ 0.5 CSS px**，所以最小可辨笔画 **≥ 8 画布 px（= 4 CSS px）**。

| 部位 | 最小尺寸 | 画布 px |
|---|---|---|
| 眼睛（直径） | 主识别区，要大 | **≥ 20 px** |
| 嘴（高度） | 大表情才画 | ≥ 6 px |
| 头部轮廓 | 必须清晰 | — |
| 尾巴宽度 | 摆动要看得见 | ≥ 12 px |

**语义必须靠这些表达**：
头部角度 / 整体姿态 / 尾巴方向 / 大表情 / 道具轮廓（如小电脑）

**不要靠这些表达**：手指动作、衣服褶皱、发丝细节、细小配饰。

> 例：`work` 不要画"手指敲键盘"，要画"抱着电脑、头一低一抬"。

---

## 4. 动画清单（10 个剪辑，128 帧）

| # | 剪辑 | 帧数 | FPS | 时长 | 循环 | 优先级 |
|---|---|---|---|---|---|---|
| 1 | `idle` | 20 | 10 | 2.0s | ✓ | **P0** |
| 2 | `run` | 12 | 16 | 0.75s | ✓ | **P0** |
| 3 | `jump` | 14 | 14 | 1.0s | ✗ | **P0** |
| 4 | `click` | 8 | 12 | 0.67s | ✗ | **P0** |
| 5 | `drag` | 12 | 10 | 1.2s | ✓ | **P0** |
| 6 | `think` | 12 | 8 | 1.5s | ✓ | **P0** |
| 7 | `work` | 12 | 10 | 1.2s | ✓ | **P0** |
| 8 | `success` | 14 | 12 | 1.17s | ✗ | **P0** |
| 9 | `error` | 12 | 10 | 1.2s | ✗ | P1 |
| 10 | `sleep` | 12 | 6 | 2.0s | ✓ | P1 |

**为什么是 10 个而不是更多**：眨眼已并入 `idle`，被抓起并入 `drag` 的开场，
落地复用 `jump` 的尾段 —— 见 §6。不需要额外的独立剪辑。

---

## 5. 帧相位分解

> 除循环剪辑外，**首帧与尾帧都应回到 `idle_001` 姿态**，衔接才不会跳。

### 1) `idle` — 20 帧 / 10fps / 循环（2.0s）

一个完整呼吸周期，**内含 2 次眨眼**（分别在第 5–6 帧、第 15–16 帧）。

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | 基准 | 自然站姿，重心略偏一侧 |
| 002–004 | 吸气上升 | 胸口抬起，肩微沉，**发梢开始上扬** |
| 005–006 | **眨眼 1** | 上眼睑下降 → 全闭（2 帧 ≈ 200ms） |
| 007 | 睁眼 | 回到吸气顶点 |
| 008–010 | 呼气下降 | 身体回落，**尾巴向右摆** |
| 011–012 | 最低点 | 略下沉，头发还在往下走（**滞后**） |
| 013–014 | 回摆 | 尾巴回摆，头发追上身体 |
| 015–016 | **眨眼 2** | 同 005–006 |
| 017–020 | 归位 | 回到 001 姿态，无缝循环 |

### 2) `run` — 12 帧 / 16fps / 循环（一个周期 = 两步）

| 帧 | 相位 |
|---|---|
| 001 | **Contact**（右脚着地） |
| 002 | **Down**（重心最低，支撑腿弯） |
| 003 | **Passing**（支撑腿直，另一腿收拢） |
| 004 | **Up**（重心最高，准备腾空） |
| 005 | **Airborne**（双脚离地） |
| 006 | **Contact**（左脚着地，进入第二步） |
| 007–010 | Down / Passing / Up / Airborne（镜像） |
| 011–012 | 过渡，接回 Contact |

要点：
- 身体**前倾 5~8°**，视线朝跑动方向
- 手臂前后摆 —— **但比例短，摆幅要小**
- **头发与裙摆比身体晚 1 帧**（飘动方向滞后）
- 锚点不变；脚底短暂离地由画布下方 32px 留白承担

### 3) `jump` — 14 帧 / 14fps / 不循环（含落地）

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | 起始 | = idle_001 |
| 002–003 | **Anticipation** | 屈膝下蹲，身体压扁（squash），手往后 |
| 004 | **Takeoff** | 腿蹬直，身体拉长（stretch），脚即将离地 |
| 005–006 | **Airborne 上升** | 双脚离地，手臂上扬，**头发/裙摆向下拖** |
| 007 | **Apex** ← `airborneFrame` | 最高点，身体略呈弧形，尾巴上扬 |
| 008–009 | **下落** | 发梢/裙摆**向上飘**（反向滞后），脚往下探 |
| 010 | **Contact** | 脚触地，身体开始压缩 |
| 011–012 | **Landing Squash** | 最大压缩（scaleY ≈ 0.88，横向略宽） |
| 013 | Rebound | 反弹过冲，身体略拉长 |
| 014 | Settle | 回到 idle_001 |

> **这一条剪辑兼任两个用途**：正常跳跃播放 001–014；
> **拖动释放时从 `airborneFrame`(007) 开始播**，得到"落下 + 落地"，
> 所以**不需要单独的 landing 剪辑**。

### 4) `click` — 8 帧 / 12fps

| 帧 | 相位 | 要点 |
|---|---|---|
| 001 | Idle | = idle_001 |
| 002 | **受惊** | 身体瞬间收缩（scaleY 0.94），眼睛睁大 |
| 003–004 | **反应** | 双手微抬，头微歪，表情变开心 |
| 005–006 | 回落 | 身体放松 |
| 007–008 | 恢复 | 回到 idle_001 |

### 5) `drag` — 12 帧 / 10fps（**开场 4 帧 + 循环段 8 帧**）

这种「开场 + 循环」结构由 manifest 的 `introFrames` 描述，
运行时播完前 4 帧后在第 5–12 帧之间循环。

| 帧 | 段 | 相位 |
|---|---|---|
| 001 | 开场 | 被抓住 —— = idle_001，但重心上提 |
| 002 | 开场 | 起吊：脚离地，身体轻微收缩（紧张感） |
| 003 | 开场 | 悬空：双腿自然下垂微弯，手微张 |
| 004 | 开场 | 稳定（= 循环段首帧姿态） |
| 005–012 | **循环** | 悬垂摆动：身体微摆，**头发与尾巴下垂并比身体晚 2 帧摆动** |

> 往哪边拖就倾向哪边，由运行时用 `rotate` 做 —— **素材画中性悬垂即可，不要画方向**。

### 6) `think` — 12 帧 / 8fps / 循环

| 帧 | 相位 |
|---|---|
| 001 | 基准站姿 |
| 002–004 | 头微歪，视线偏上，一手抬到胸前/托腮 |
| 005–008 | 保持思考姿态，**尾巴缓慢左右摆**，轻微上下浮动 |
| 009–012 | 头回到正中再微歪，形成循环 |

### 7) `work` — 12 帧 / 10fps / 循环

**不要画手指敲键盘**（104px 下看不见）。用**大姿态**表达：

| 帧 | 相位 |
|---|---|
| 001 | 抱出一台小笔记本电脑 |
| 002–004 | 坐下 / 半蹲，把电脑放稳 |
| 005–010 | **头一低一抬的节拍循环**（电脑保持，头部起伏 2 帧一拍） |
| 011–012 | 抬眼看一眼，接回 005 |

### 8) `success` — 14 帧 / 12fps / 不循环

| 帧 | 相位 |
|---|---|
| 001 | = idle_001 |
| 002–003 | 蓄力下蹲 |
| 004–008 | **跳起 + 双手高举**（可复用 jump 腾空段，但手臂高举） |
| 009–011 | 落地压缩 |
| 012–013 | 开心笑，**头顶带 2~3 颗小星星**（画在帧里，不要运行时生成） |
| 014 | 回到 idle_001 |

### 9) `error` — 12 帧 / 10fps / 不循环

| 帧 | 相位 |
|---|---|
| 001 | = idle_001 |
| 002–003 | **愣住**（身体僵一下） |
| 004–006 | 头顶冒出 `!`，眼睛变困惑 |
| 007–009 | 歪头，双手微摊（「诶？」） |
| 010–012 | 回到 idle_001 |

### 10) `sleep` — 12 帧 / 6fps / 循环

| 帧 | 相位 |
|---|---|
| 001–004 | 打哈欠，闭眼 |
| 005–008 | 头垂下，身体放松下沉 |
| 009–012 | 缓慢呼吸起伏（幅度大于 idle），尾巴偶尔抖一下 |

---

## 6. 二级运动：必须烘焙进帧里

v1 runtime 不分层，**头发 / 裙摆 / 尾巴的滞后与跟随要画在帧序列里**。
这一条是「活起来」与「一张纸在晃」的分界线，**按硬要求对待**。

| 剪辑 | 要求 |
|---|---|
| `idle` | 发梢比身体晚 1 帧到达最高点；尾巴独立摆动 |
| `jump` | 上升时头发/裙摆**向下拖**，下落时**向上飘**（反向滞后） |
| `jump` 落地段 | 落地瞬间**裙摆还在往下压**，比身体晚 1~2 帧回弹 |
| `run` | 头发/裙摆飘动方向比身体晚 1 帧 |
| `drag` | 头发与尾巴**下垂**，比身体晚 2 帧摆动 |
| `success` | 跳起时裙摆上翻，落地时滞后下压 |

---

## 7. 透明与边缘

❌ **不要**：白底 / 灰底 / **棋盘格**（最危险 —— 会被当成真实像素）、
角色外圈的柔光 / 光晕 / 渐隐阴影、投影。
✅ **要**：完全透明背景、清晰描边、发梢等半透明处保留正确 alpha。

> 实测：图像模型常把"透明背景"理解成**画出棋盘格**。
> **请导出自带 alpha 通道的 PNG**；或先出纯平面白底（不能有光晕）由我统一抠。

---

## 8. 只出一套朝向

| 剪辑 | 需要左右两套吗 | 说明 |
|---|---|---|
| `run` | **否** | 只出**右向**，左向由运行时 `scaleX(-1)` 翻转 |
| 其余 | **否** | 均为正面 / 对称姿态 |

**所以是 10 个剪辑，不是 ×2。**

---

## 9. 文件与 manifest

```
assets/pet/
  manifest.json
  idle.png  run.png  jump.png  click.png  drag.png
  think.png work.png success.png error.png sleep.png
```

```json
{
  "version": 2,
  "canvas":  { "w": 256, "h": 320 },
  "anchor":  { "x": 0.5, "y": 0.9 },
  "charHeightPx": 208,
  "charRatioInCanvas": 0.65,
  "displayCharHeightCss": 104,
  "columns": 8,
  "clips": {
    "idle":    { "frames": 20, "fps": 10, "loop": true },
    "run":     { "frames": 12, "fps": 16, "loop": true,  "flipForLeft": true },
    "jump":    { "frames": 14, "fps": 14, "loop": false, "airborneFrame": 7 },
    "click":   { "frames": 8,  "fps": 12, "loop": false },
    "drag":    { "frames": 12, "fps": 10, "loop": true,  "introFrames": 4 },
    "think":   { "frames": 12, "fps": 8,  "loop": true },
    "work":    { "frames": 12, "fps": 10, "loop": true },
    "success": { "frames": 14, "fps": 12, "loop": false },
    "error":   { "frames": 12, "fps": 10, "loop": false },
    "sleep":   { "frames": 12, "fps": 6,  "loop": true }
  }
}
```

---

## 10. 验收标准（我拿到素材后逐条检查）

1. 全部 PNG **RGBA**，四角 `alpha = 0`（无白底 / 棋盘格 / 光晕残留）
2. 每帧严格 **256×320**，帧数符合 §4
3. **所有帧**脚底 `y = 288 ± 4`、水平中心 `x = 128 ± 6`
4. 角色高度 ≈ **208 px（65%）**；整体 bounding box 宽 **≤ 166 px**
5. **头身比 1.7~1.9**（头高 ≈ 0.55 H）
6. 发梢**不拖到脚**；尾巴**未撑宽 bounding box**
7. 循环剪辑首尾同姿；非循环剪辑首尾回到 `idle_001`
8. `idle` 肉眼可见呼吸 + 尾巴摆动 + 2 次眨眼
9. `run` 有可辨的两步循环与腾空帧
10. `jump` 有下蹲 / 拉长 / 腾空 / 触地压缩四个可辨阶段
11. **缩到 104px 显示时**，眼睛、头部动作、尾巴摆动仍然清晰可读
12. 二级运动在 `jump` / `run` / `drag` 中肉眼可辨

任何一条不满足我都会指出来，**不会用低质量素材硬凑**。

---

## 11. 交付批次

| 批次 | 内容 | 说明 |
|---|---|---|
| **第 1 批** | `idle` `think` `work` | 3 个剪辑就能跑通"平时活着 + 两种工作状态" |
| **第 2 批** | `click` `jump` `drag` | 交互层（含拖动释放，靠 jump 尾段） |
| **第 3 批** | `run` `success` `error` `sleep` | 位移与结果反馈 |

**先给第 1 批我就能开始接 Runtime**，不必等齐。

---

## 12. 前置说明

第 1 批到位前，**runtime 继续用现有占位素材**（已按新画布几何重排：
256×320 画布、角色 208px、脚底 y=288），只做静态展示，不做任何动画拼凑。

新素材到位后我会先跑 §10 验收，再进 S2 Animation Runtime。
